// simple if and if selse
let a=20;
if(a>0)
{
    console.log('a is +ve')
}
if(a>0)

    console.log('a is =ve ');
    else
    console.log('a is negative');

    let c=10;
    let b=10;
    if(a>b)
    console.log(`Max:${a} \nMin:${b}`);
    else
    console.log(`Max:${b} \nMin:${a}`);
